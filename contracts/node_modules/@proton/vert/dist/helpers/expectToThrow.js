"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.expectToThrow = void 0;
const chai_1 = require("chai");
/**
 * Expect a promise to throw an error with a specific message.
 * @param promise - The promise to await.
 * @param {string} errorMsg - The error message that we expect to see.
 */
const expectToThrow = async (promise, errorMsg) => {
    try {
        await promise;
        throw new Error(`Was expecting to fail with ${errorMsg}`);
    }
    catch (e) {
        (0, chai_1.expect)(e.message).to.be.deep.eq(errorMsg);
    }
};
exports.expectToThrow = expectToThrow;
//# sourceMappingURL=expectToThrow.js.map